var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.send("//todo: Need to migrate the registration Service");
});

module.exports = router;
